# Tasker (Docstore) — npm wrapper

This package downloads the prebuilt `tasker` CLI from GitHub releases.

## Install
```bash
npm install -g @amirbrooks/tasker-docstore
```

## Usage
```bash
tasker --help
tasker init --project "Work"
```

## Notes
- Requires a matching Git tag `v<version>` in the GitHub releases.
- For manual install, see the main repo README.
